spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: reserva
          uri: lb://RESERVA-SERVICE
          filters:
            - StripPrefix=1
          predicates:
            - Path=/reserva/**

eureka:
  client:
    service-url:
      defaultUrl: http://localhost:8761/eureka

server:
  port: 9098
